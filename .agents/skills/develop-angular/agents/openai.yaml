interface:
  display_name: "Dev Angular"
  short_description: "Angular / TypeScript / Tailwind development"
  brand_color: "#DD0031"
  default_prompt: "Implement the requested Angular changes with TypeScript strict mode and Tailwind CSS."

policy:
  allow_implicit_invocation: true

model:
  name: "claude-opus-4-6"
  provider: "anthropic"
  parameters:
    temperature: 0.1
    top_p: 0.95
    max_tokens: 16384
    reasoning_effort: "high"
